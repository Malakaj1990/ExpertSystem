
import ProjectManager

engine = ProjectManager.ProjectManaer()
engine.reset()  # Prepare the engine for the execution.
engine.run()  # Run it!
FactList = engine.FactsMap
for k, v in FactList.items():
    v.printFactContainer()

